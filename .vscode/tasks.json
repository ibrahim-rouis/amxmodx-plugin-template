{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build: Compile open script",
            "command": "${command:amxxpawn.compile}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}